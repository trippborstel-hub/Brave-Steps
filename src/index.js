import React from 'react';
import ReactDOM from 'react-dom/client';
import BraveSteps from './BraveSteps';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BraveSteps />
  </React.StrictMode>
);
